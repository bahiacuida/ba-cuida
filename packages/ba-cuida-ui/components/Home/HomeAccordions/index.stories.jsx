import { HomeAccordions } from './index'

const meta = {
  title: 'Pages / Home / HomeAccordions',
  component: HomeAccordions,
  parameters: {
    layout: 'fullscreen',
  },
}

export default meta
export const Basic = {}
