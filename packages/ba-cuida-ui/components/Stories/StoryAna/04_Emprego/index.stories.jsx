import { Emprego } from './index'

const meta = {
  title: 'Pages / Home / StoryAna / Emprego',
  component: Emprego,
  parameters: {
    layout: 'fullscreen',
  },
}

export default meta
export const Basic = {}
