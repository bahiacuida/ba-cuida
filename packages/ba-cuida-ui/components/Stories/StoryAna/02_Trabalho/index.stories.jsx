import { Trabalho } from './index'

const meta = {
  title: 'Pages / Home / StoryAna / Trabalho',
  component: Trabalho,
  parameters: {
    layout: 'fullscreen',
  },
}

export default meta
export const Basic = {}
