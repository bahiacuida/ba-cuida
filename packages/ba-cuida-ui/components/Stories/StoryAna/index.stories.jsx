import { StoryAna } from './index'

const meta = {
  title: 'Pages / Home / StoryAna',
  component: StoryAna,
  parameters: {
    layout: 'fullscreen',
  },
}

export default meta
export const Basic = {}
