import { Previdencia } from './index'

const meta = {
  title: 'Pages / Home / StoryEugenia / Previdencia',
  component: Previdencia,
  parameters: {
    layout: 'fullscreen',
  },
}

export default meta
export const Basic = {}
