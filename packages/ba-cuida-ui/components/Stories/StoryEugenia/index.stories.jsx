import { StoryEugenia } from './index'

const meta = {
  title: 'Pages / Home / StoryEugenia',
  component: StoryEugenia,
  parameters: {
    layout: 'fullscreen',
  },
}

export default meta
export const Basic = {}
