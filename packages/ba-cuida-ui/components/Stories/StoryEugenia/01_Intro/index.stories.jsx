import { Intro } from './index'

const meta = {
  title: 'Pages / Home / StoryEugenia / Intro',
  component: Intro,
  parameters: {
    layout: 'fullscreen',
  },
}

export default meta
export const Basic = {}
