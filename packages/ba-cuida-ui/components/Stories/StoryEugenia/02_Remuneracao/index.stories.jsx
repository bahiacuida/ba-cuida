import { Remuneracao } from './index'

const meta = {
  title: 'Pages / Home / StoryEugenia / Remuneracao',
  component: Remuneracao,
  parameters: {
    layout: 'fullscreen',
  },
}

export default meta
export const Basic = {}
