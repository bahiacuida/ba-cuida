import { PaperScrap } from './index'

const meta = {
  title: 'Components / PaperScrap',
  component: PaperScrap,
}

export default meta
export const Basic = {
  args: {
    children: <div>Hello world</div>,
  },
}
